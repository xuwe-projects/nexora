# Nexora 0.24.1

## 修复

- Windows NSIS 打包在生成 `FileVersion` 时兼容日期型或流水型大 `build_number`。更新清单、产物路径和版本比较仍保留完整 `build_number`，仅 Windows 资源文件按格式限制写入 16-bit 版本段。— 处理人：[@openai](https://github.com/openai)

## 验证

- `cargo fmt --all`
- `cargo test -p nexora --test build --features cli -j 2`

