---
title: Console 0.26.0
---

- 更新完成 Dialog 展示经过校验的版本、构建号和 Markdown 版本日志，并统一立即重启、稍后重启
  与健康确认后的完成状态。
- 登录页版本信息优先使用正式发布元数据；缺失元数据的 `cargo run` 和测试继续使用开发模式
  fallback。
- 正式构建需要维护 `release.notes` 并使用 0.26.0 CLI 重建，以便安装包携带发布身份与日志。
