## 桌面组件与示例

- 文档站新增“组件”入口，组件参考页现在包含快速开始、组件总览、API 表和基础示例。
- 默认 Account 用户与角色管理筛选区域改用统一的 `LabeledControl` 字段容器，标签、说明和
  组件尺寸会跟随主题设置保持一致。
- 仓库提供 `desktop_basic` 和 `server_basic` 官方 Cargo example target，便于从最小示例验证
  桌面 Shell、CRUD Panel、表单字段和服务端 Router 组合。
