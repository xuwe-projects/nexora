---
title: API 0.36.0
---

- 新增 `CrudQuery` 派生契约，为分页、筛选、排序和校验提供强类型元数据，同时保持应用原有 HTTP wire 字段不变。
- `CrudTableRow` 的可排序列必须显式映射服务端排序枚举，避免桌面列名意外泄漏到 API。
- `CrudListState` 支持按请求解析应用上下文，适配认证、工厂作用域和其他运行期请求依赖。
