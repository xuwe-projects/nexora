## 服务端客户门户能力

- Nexora 0.11.0 增加 ZITADEL 客户门户 provisioning API 和独立 Bearer token verifier。
- `VerifiedIdentity` 新增 `organization` 字段；自定义测试 verifier 使用结构体字面量时需要补齐。
- 本版本不改变桌面 UI、默认 Account 管理页面或生成项目 feature 使用方式。
