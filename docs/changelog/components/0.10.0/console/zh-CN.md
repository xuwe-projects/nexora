## 标准 FormDialog 与设置联动

- 新增 `FormItem` / `FormItemControl`，创建和编辑表单统一使用 builder API 组合标准字段与自定义
  section。
- 顶部标签栏默认同步 gpui-component 官方 `TabBar` 的 `Tabs` 样式，并可显式选择 `Segmented` 等官方变体。
- 默认用户管理、角色与权限页面补齐筛选/查询，按钮和表单控件统一跟随设置里的组件尺寸。
- 设置窗口新增默认显示器选项；生成项目 skill 已同步 CRUD Panel、FormDialog 和组件尺寸规则。
