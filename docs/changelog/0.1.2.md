# Nexora 0.1.2

发布日期：2026-07-17

本版本完善文档产品展示和可重复的 GitHub 发布流程，不引入新的框架 API 破坏性变更。

## Added

- 增加由工作区、外观设置与登录页组成的响应式产品总览，三组卡片分别组合浅色和深色原始
  截图，避免在首页逐张堆放且保持 UI 文字不被重绘。— 处理人：
  [@Jixiangup](https://github.com/Jixiangup)
- 增加 `publish-nexora-release` Skill，规范版本清点、处理人和 Issue 归属、升级文档、验证、
  tag 与 GitHub Release 创建流程；`nexora create/init` 会把它写入生成项目。— 处理人：
  [@Jixiangup](https://github.com/Jixiangup)

## Changed

- 将完整版本说明与应用内组件 Changelog 迁入 VitePress 文档，中文和英文导航都可以按版本
  访问。— 处理人：[@Jixiangup](https://github.com/Jixiangup)
- README、快速开始和 CLI 参考同时提供本地源码与 GitHub tag 安装命令，并使用兼容 Bash、
  zsh、PowerShell 和 CMD 的单行形式。— 处理人：[@Jixiangup](https://github.com/Jixiangup)

## Fixed

- 修正 Git 仓库包含多个二进制 package 时的 CLI 安装命令，使用位置参数 `nexora` 明确选择
  package。— 处理人：[@Jixiangup](https://github.com/Jixiangup)
- 修正文档首页对 Server 资源所有权的旧描述，明确连接池、监听器与生命周期由宿主应用管理。
  — 处理人：[@Jixiangup](https://github.com/Jixiangup)

## 关联 Issue / PR

无。本版本没有把未实际处理的开放 Issue 计入发布说明。

## 兼容性

从 `0.1.1` 升级到 `0.1.2` 没有框架公开 API、配置或数据库结构的破坏性变更，无需手工数据
迁移。

## 从 0.1.1 升级

重新安装已发布 CLI：

```bash
cargo install --git https://github.com/xuwe-projects/nexora --tag v0.1.2 nexora --locked --force --no-default-features --features cli --bin nexora
```

已有项目把 workspace 中的 Nexora Git tag 更新为 `v0.1.2`，然后更新锁文件：

```toml
nexora = { git = "https://github.com/xuwe-projects/nexora", tag = "v0.1.2", default-features = false }
```

```bash
cargo update -p nexora
```

如需把新的发布 Skill 写入已有项目，使用重新安装后的 CLI 执行 `nexora init`，并传入项目
当前使用的布局；命令只补充缺失文件，不覆盖已有规则或 Skill。

## 验证

- `cargo test --workspace --all-targets`
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`
- `cargo run -p nexora -- lint --workspace . --deny-warnings`
- `bun install --frozen-lockfile && bun run build`

依赖真实 PostgreSQL、OIDC 或签名凭据的外部环境验证不包含在本次文档与发布流程改动中。
